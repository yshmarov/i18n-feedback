/.bundle/
/vendor/bundle
/pkg/
/tmp/
/log/*.log
/spec/dummy/log/*.log
/spec/dummy/tmp/
/spec/dummy/db/*.sqlite3
/spec/examples.txt
Gemfile.lock
gemfiles/*.lock
.DS_Store
*.gem

# Full-resolution demo recording — too large for GitHub (>100MB).
# The committed GIF and the 640px MP4 are the shareable versions.
/i18n-feedback-demo.mp4
